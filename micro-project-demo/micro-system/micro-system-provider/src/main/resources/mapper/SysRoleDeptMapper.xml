<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.atqgh.mapper.SysRoleDeptMapper">

	<!-- 结果集映射 -->
    <resultMap id="sysRoleDeptMap" type="com.atqgh.dto.SysRoleDeptDto" >
        <result property="roleCode" column="role_code"/>
        <result property="deptCode" column="dept_code"/>
    </resultMap>

    <!-- 结果集映射 -->
    <resultMap id="sysRoleDeptDetailMap" type="com.atqgh.dto.SysRoleDeptDto" >
        <result property="roleCode" column="role_code"/>
        <result property="deptCode" column="dept_code"/>
    </resultMap>

    <!-- 字段分片 -->
    <sql id="Base_Column_List">
        role_code,
        dept_code
    </sql>

    <!-- 字段分片 -->
    <sql id="Base_Column_Detail">
        role_code,
        dept_code
    </sql>

    <!-- 分页字段分片 -->
    <sql id="Base_Column_Page">
        role_code AS roleCode,
        dept_code AS deptCode
    </sql>

</mapper>